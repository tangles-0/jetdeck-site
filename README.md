
  # JetDeck SCOUT Cyberdeck Launch Site

  This is the JetDeck SCOUT Cyberdeck Launch Site.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  